# 待办清单

这份清单记录已经讨论确定、但还没做完的事。做完的会移到最下面的"已完成"。

---

## A. Bug（影响信任，优先修）

### ✅ A1 基础水平没生效
`courseFactor` 定义了但从来没被读过，选"我考过"照样排全部课程。
**已修**：选完基础水平会折算课节数，并且填完课看清会看到"因为你说 X，课按 Y% 折算过"。

### ✅ A2 感受调整看不见
选"太难了"只减 1 分钟。两个原因：感受只作用在"听课和申论之后的剩余"上，那部分本来就小；题量按 5 的倍数取整，把细微变化吃掉了。
**已修**：感受只作用于弹性部分（刷题、复盘），题量步长从 5 改成 1。现在太轻松 32→34 题、有点累 32→29 题、太难了 32→26 题。

### ⬜ A3 听课和刷题用了同一套顺延逻辑
- **听课是线性的**——今天第 3 节没听，明天必须接着听，不能跳，所以未完成必须顺延
- **刷题是弹性的**——今天 25 题没做，明天做或者不做都行，直接放弃才对

现在两种情况走同一套逻辑，导致刷题会莫名其妙堆到后面几天。改法：`engine.js` 的 `carryOver` 按 `kind` 分开处理，`course` 顺延、`practice` 直接丢。

### ⬜ A4 常识权重
常识性价比最低，很多人直接不学。权重已从 1 压到 0.6，还要做的是：
- 设置页里给常识一个显眼的"不学"入口
- 问卷或确认页上说明一句"常识靠平时积累，专门刷题收益很小，很多人直接放弃"

---

## B. 数据地基（三个要一起做，动数据结构）

这三件事共用同一层数据，分开做会返工。

### ⬜ B1 打卡记录改成追加式
现在是直接改任务状态（`task.status = 'done'`），等于覆盖历史。改成每次打卡追加一条 `{任务, 状态, 时间}`，任务上的状态只是"最新一条的结果"。

好处：将来做多设备同步不会冲突；学习档案需要"最后一节是什么时候听的"这种时间线；还能看出"他是晚饭后做的还是半夜做的"。

### ⬜ B2 正确率录入
入口放统计页顶部，一个"记录一次成绩"按钮。分九个模块填正确率，**只填手上有的，其他留空**。刚做完模考照着粉笔的报告抄一遍，一分钟的事。

这个数据驱动三件事：
- 复盘时长（见 B3）
- 模块强度建议（"资料分析 78%，建议减少；言语理解 42%，建议加强"）
- **区分"练得少"和"练了没用"**——连续三次正确率不动，说明是方法问题，该回去重听课，这时候加量是没用的

### ⬜ B3 复盘时长改由错误率算
现在是拍脑袋的"刷题时长 × 40%"。改成：

```
复盘时长 = 刷题时长 × 错误率 × 复盘系数
```

默认错误率 30%、复盘系数 1.4，算出来正好 42%，和现在的 40% 基本一致——所以默认行为不变，改的是它之后能被用户真实数据替换。

---

## C. 核心功能

### ⬜ C1 学习档案
常驻在统计页，断更回来时自动弹出。**第一个数字必须是"你完成了什么"，不是"你欠了多少"。**

```
你上次学习是 10 天前。这段时间你完成的

听课
  资料分析    ████████ 4/4 节  已完成 ✓
  言语理解    ████████ 5/5 节  已完成 ✓
  判推逻辑    ████░░░░ 2/4 节  听到一半

刷题
  资料分析    累计 320 题

建议先回顾这些
□ 资料分析    最后学的，隔了 10 天，重做 10 道
□ 判推逻辑    第 3 节听到一半，接着听
```

结尾给一个动作（"就按这个来"），不是一堆选项。

### ⬜ C2 半自动模式
给当天的每个任务加三个动作：

| 用户动作 | 系统怎么做 |
| --- | --- |
| 今天不做这项 | 不计入失败，时长记为结余，后面几天适当放松 |
| 今天想多做点 | 记为超额，后面 1–2 天减量补偿 |
| 换成别的模块 | 改今天的任务，同时调整后面的权重 |

**三个动作都不进完成率。** "我不想"和"我做不到"是两种信号，前者要的是尊重，后者才是减量。

换模块的列表用**六板块 + 申论**（资料 / 言语 / 判断推理 / 数量 / 常识 / 政治理论 / 申论）。判断推理下面给二级选择（逻辑 / 图形 / 定义类比，默认"都可以"）。

连续三次选同一个板块 → 系统主动问"要不要加进日常安排"。
连续跳过 3 天 → 提示一次，不惩罚。

### ⬜ C3 使用模式三档
开始让用户选一次：全自动 / 半自动 / 自己排。选了之后界面只显示那一档需要的东西。

核心是**"建议 vs 实际"两层数据**：用户改的是"实际"，"建议"保留不动。系统往后排按"实际"算，统计页能对比两者。

这顺带产生的是一份很有价值的诊断数据：**用户的偏离模式**——他老是在周几跳过、老是把什么换成什么。

### ⬜ C4 阶段按完成条件推进
现在阶段按日期切，到了就换，不管内容做完没。改成按完成条件：

> 所有模块的课听完 **且** 至少 5 个模块的专项过完一轮 → 进入套卷阶段

进度快就提前，慢就延后。

### ⬜ C5 进度预测
> 按现在的节奏，你会在 10 月 12 日听完所有课，11 月 3 日完成第一轮专项，12 月 1 日开始套卷。

用剩余课时 ÷ 每日实际推进速度就能算。会随真实进度动态变化——进度快了看到日期提前，这本身就是动力。

### ⬜ C6 主动建议取舍
时间不够时系统必须主动说：

> "还差资料分析和数量关系的专项没做完。距离考试还有 21 天。建议：先补资料分析（占分高、提分快），数量关系直接放弃专项，只做套卷里遇到的题。"

**这是这个产品最有价值、市面上也最没人做好的地方——不是排计划，是在时间不够的时候替你砍东西。**

### ⬜ C7 刷题按台阶推进
不用数量界定"够不够"，用正确率：

```
资料分析
├─ 台阶1  基础题型过一遍        目标：题型认得出
├─ 台阶2  专项强化              目标：正确率 70%
├─ 台阶3  限时提速              目标：稳定在 75% 且不超时
└─ 台阶4  每天 20 题保持手感
```

推进条件是正确率达标，不是刷够多少题。

套卷效果不好时，系统主动提议回炉："资料分析这套只有 40%，比平时低了三成。要不要下周先回去练专项？"

---

## D. 体验

### ✅ D1 问卷确认页
填完课看清之后加了一块"按现在的填写，你的计划会是这样"，显示要听的课、三个阶段各多少天，并说明折算原因。

### ⬜ D2 "不需要听课"选项
功能上已经能做（课节数填 0），缺的是引导。跟 A1 一起修：明确给一个选项，选了之后课节数归零，所有时间给刷题。

### ⬜ D3 进度条改按任务项算
现在按分钟算，但分钟数是估的（每题用时是估的，复盘时长是推的），拿它当分母会让完成度失真。改成"今天 3 项，完成 2 项"，时长作为参考显示在旁边。

---

## E. 已确认的判断（不是待办，是设计约束）

- **常识性价比最低，很多人不学**——权重最低，允许一键设成"不学"
- **半自动的科目粒度是六板块 + 申论**
- **错题用户自己在题本上打×**，系统不需要记具体题目，回顾任务写"资料分析，重做 10 道"就够
- **长期不上线回来，第一屏先说"你完成了什么"**，再说"要复习什么"
- **没有任何人会完全照着一个程序给的方案学习**——偏离是常态，系统要能接住
- **AI 负责"怎么说"，代码负责"排什么"**——排计划不能交给模型

---

## 已完成

- 申论独立成一条线，按本周总时长 × 35% 给预算，预算可滚存，听课和练题交替
- 行测刷题按性价比加权（资料/言语/判断 2.5，政治 1.2，数量 1，常识 0.6）
- 计划页三档视图：本周展开、两周折叠、本月日历
- 高级参数可在设置里改，默认值即推荐值
- 计划页、统计页、设置页、打卡、感受微调
